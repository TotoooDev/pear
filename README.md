# pear

a cool 3d engine

## projects

- `pear` is the engine itself
- `pear-3d` is the (crude) library to load and save the custom 3d file format
- `pear-project` is the game that uses pear as the engine
